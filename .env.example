# Phase 1 — Public feature flags
NEXT_PUBLIC_HIRE_STATUS=available
NEXT_PUBLIC_SHOW_SALARY=true

# Phase 2 — AI chatbot (server-side only)
OPENAI_API_KEY=

# PostgreSQL + pgvector (RAG documents table)
DATABASE_URL=postgresql://user:password@localhost:5432/saas_portfolio
# DATABASE_SSL=true

# Redis — rate limiting (Upstash REST or Redis URL)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# REDIS_URL=redis://localhost:6379
